<Window x:Name="MainWin" x:Class="ZipImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZipImageViewer"
        mc:Ignorable="d" Background="#FF1F1F1F" UseLayoutRounding="True"
        Title="ZipImageViewer" Height="640" Width="1030" Drop="MainWin_Drop" AllowDrop="True" Loaded="MainWin_Loaded">
    <ScrollViewer>
        <ItemsControl x:Name="LB1" ItemsSource="{Binding ElementName=MainWin, Path=ImageList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Width="{Binding ElementName=LB1, Path=ActualWidth, Mode=OneWay}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel x:Name="SP1" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center"
                                UseLayoutRounding="True">
                        <local:Thumbnail x:Name="TN1" Tag="False" RenderTransformOrigin="0.5 0.5" Panel.ZIndex="1"
                                         ImageInfo="{Binding}" Margin="10"
                                         PreviewMouseLeftButtonUp="TN1_PreviewMouseLeftButtonUp">
                            <!-- Width="{Binding Source={x:Static Application.Current}, Path=ThumbnailSize.Width}" -->
                            <!-- Height="{Binding Source={x:Static Application.Current}, Path=ThumbnailSize.Height}" -->
                            <local:Thumbnail.Effect>
                                <DropShadowEffect Direction="270" Opacity="0.8" ShadowDepth="1" BlurRadius="3"/>
                            </local:Thumbnail.Effect>
                            <local:Thumbnail.RenderTransform>
                                <ScaleTransform/>
                            </local:Thumbnail.RenderTransform>
                            <local:Thumbnail.Style>
                                <Style TargetType="local:Thumbnail">
                                    <Style.Triggers>
                                        <Trigger Property="Tag" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard Storyboard="{StaticResource SB_ZoomIn}"/>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard Storyboard="{StaticResource SB_ZoomOut}"/>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </local:Thumbnail.Style>
                        </local:Thumbnail>
                        
                        <TextBlock Text="{Binding FileName}" FontSize="10" Foreground="Gray"
                                   HorizontalAlignment="Center" Panel.ZIndex="0"/>
                    </StackPanel>
                    <DataTemplate.Triggers>
                        <Trigger SourceName="SP1" Property="IsMouseOver" Value="True">
                            <Setter TargetName="TN1" Property="Tag" Value="True"/>
                        </Trigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="{x:Type ContentPresenter}">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Panel.ZIndex" Value="9999" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </ScrollViewer>
    
</Window>



